# Minimal Prometheus configuration for demo
global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  # Scrape Prometheus itself
  - job_name: 'prometheus'
    static_configs:
      - targets: ['localhost:9090']
  
  # Scrape node-exporter for real CPU, memory, disk, network metrics
  - job_name: 'node'
    static_configs:
      - targets: ['node-exporter:9100']
